# Color controls decision log

The design spec is the authority. This log records choices made while executing the plan on 2026-09-25.

| Date and task | Decision | Alternatives considered | Why | Revisit? |
| --- | --- | --- | --- | --- |
| 2026-09-25 · Setup | Keep all four slices on `color-controls`, with slice checkpoint commits and tags. | Four stacked branches and PRs from the plan. | The handoff explicitly chooses one branch and says Maggie will open the PR. | No. |
| 2026-09-25 · Setup | Work in `/private/tmp/chopin-color-controls`. | The requested sibling path; edits in the detached starting checkout. | The sibling path is outside this task's writable sandbox; the branch and base commit stay as requested. | No. |
| 2026-09-25 · Setup | Use ordinary commit attribution without the plan's Claude co-author line. | Copy the plan's footer. | The handoff explicitly replaces that attribution. | No. |
| 2026-09-25 · Setup | Run the full `bun test` check with local socket permission. | Run in the default sandbox. | Baseline socket tests fail to bind port 0 in the default sandbox; with socket permission the baseline is 1511 passed, 2 PostgreSQL skips, 0 failed. | No. |
| 2026-09-25 · Browser setup (superseded) | Initially opened the signed-in 8787 tab, then closed it after Maggie clarified that it belongs to another agent. | Reuse the active server's database and port. | The active server owns port 8787 and the database writer lease; a second server should not clear its process-local sessions. | Yes, if the branch specimen needs server access. |
| 2026-09-25 · Task 1 implementer model | Use Luna. | Terra or Sol. | The plan gives complete test and conversion code; package scaffolding and transcription are bounded mechanical work. | No. |
| 2026-09-25 · Task 1 reviewer model | Use a fresh Sol reviewer. | Terra or Luna. | The color conversion API is foundational; the review needs careful spec and numerical edge-case judgment. | No. |
| 2026-09-25 · Task 1 fix reviewer model | Use a fresh Sol reviewer for the parser fix. | Reuse the original reviewer or use Terra. | The overflow fix sits on a foundational parsing boundary; fresh reasoning checks the regression without inheriting the first verdict. | No. |
| 2026-09-25 · Task 2 implementer model | Use Terra. | Luna or Sol. | Gamut search and WCAG arithmetic are specified with tests, but numerical edge cases merit a mid-tier implementer. | No. |
| 2026-09-25 · Isolated browser runtime | Use a separate Compose PostgreSQL instance on 55445 and branch dev server on 18887 with Vite on 15174. | Reuse the other agents' 8787 or 8790 servers; share their database. | Process cwd checks showed 8787 and 8790 belong to other worktrees. Dedicated ports and database avoid session and writer-lease interference. | No. |
| 2026-09-25 · Isolated browser sign-in | Use the branch's unauthenticated dev-only design-audit page on 15174, as Maggie chose. | Reuse the `e2e` client ID from the copied `.env`; alter another agent's OAuth setup. | The copied config redirects GitHub OAuth to an invalid `e2e` client, while direct Vite serves the design audit without sign-in. | No for this sub-project; authenticated routes are out of scope. |
| 2026-09-25 · Task 2 reviewer model | Use a fresh Sol reviewer. | Terra or Luna. | The review must judge numerical gamut boundaries and a threshold formatter that must never misstate compliance. | No. |
| 2026-09-25 · Task 2 formatter | Depart from the plan's `+ 1e-9` truncation formula; require a representation that preserves `1.15:1` without promoting a value below 3, 4.5, or 7. | Keep the supplied epsilon formula; round normally. | The review demonstrated `2.999999999999` displays as `3.00:1` although `passes` is false; the spec explicitly forbids that. | No. |
| 2026-09-25 · Main update | Merge `origin/main` at c62636ae into `color-controls` as c60805d before continuing Task 2. | Rebase already reviewed task commits; keep working on the older base. | Maggie requested the latest design-system changes. The merge was clean, preserves reviewed commits, and passed all required checks. | No. |
| 2026-09-25 · Task 2 fix reviewer model | Use a fresh Sol reviewer for the ratio boundary fix. | Reuse the first reviewer or use Terra. | The correction is numerically subtle and must be checked against representable values immediately below WCAG thresholds. | No. |
| 2026-09-25 · Task 2 second fix reviewer model | Use another fresh Sol reviewer for decimal truncation. | Reuse a prior reviewer or use Terra. | The first fix still promoted adjacent floats; this review checks the new representation across the valid 1–21 ratio domain. | No. |
| 2026-09-25 · Task 3 implementer model | Use Terra. | Luna or Sol. | Plane, gamut, and keyboard geometry are specified with tests, but mapping and floating-point boundaries need careful implementation. | No. |
| 2026-09-25 · Task 3 RED order | Accept the first RED failure on the undeclared required `@chopin/color` dependency, then add that dependency to reach the missing geometry module. | Expect the missing module error before declaring the dependency. | The test imports `@chopin/color` first, so dependency resolution fails before the geometry import; this does not weaken the test-first sequence. | No. |
| 2026-09-25 · Task 3 reviewer model | Use a fresh Sol reviewer. | Terra or Luna. | The geometry feeds pointer and keyboard interactions; review needs independent judgment on gamut and boundary behavior. | No. |
| 2026-09-25 · Task 3 hue endpoint | Depart from the plan's normalization-only `huePosition` so the exact 360° endpoint maps to the top. | Keep normalization-only positioning; make the top return 0°. | The spec says 360° is at the top and 0° at the bottom. The original formula placed a top-edge drag's thumb at the bottom; if this ruling is wrong, the thumb can still jump at the endpoint. | No. |
| 2026-09-25 · Task 3 fix reviewer model | Use a fresh Sol reviewer for the hue endpoint correction. | Reuse the first reviewer or use Terra. | The fix changes a user-visible slider endpoint and must preserve keyboard wrap. | No. |
| 2026-09-25 · Task 4 implementer model | Use Sol. | Terra or Luna. | The contrast reader combines visible verdicts, controlled selections, debounce announcements, and established design language. | No. |
| 2026-09-25 · Task 4 live announcement | Skip only the initial live-region effect, then announce every settled change, including a later return to the initial reading. | Follow the plan's permanent comparison against the initial message. | The spec requires announcements after a value settles; the plan's guard would suppress a valid later change. | No. |
| 2026-09-25 · Task 4 reviewer model | Use a fresh Sol reviewer. | Terra or Luna. | The reader combines accessibility timing and visual state, so its review needs UI and behavior judgment. | No. |
| 2026-09-25 · Task 4 Effect audit | Keep the 500 ms timer Effect, using `[message]` and timer cleanup, after applying Maggie's requested `review-use-effect` skill. | Derive the announcement during render; move it to individual input handlers. | Debouncing screen reader output synchronizes with an external timer and live region; cleanup handles rapid changes and unmount, and the mount guard handles Strict Mode. Browser timing remains to verify in the integrated specimen. | Yes, after Task 7 browser checks. |
| 2026-09-25 · Task 4 fix reviewer model | Use a fresh Sol reviewer for select option uniqueness. | Reuse the first reviewer or use Terra. | The change must retain callback decoding while satisfying the repository's global option-value invariant. | No. |
| 2026-09-25 · Task 5 implementer model | Use Sol. | Terra or Luna. | The field combines a draft editing state, lossy HEX display, validity feedback, and commit/escape interactions. | No. |
| 2026-09-25 · Task 5 field state | Derive the displayed value from props while idle and store only an active draft, applying `review-use-effect`. | Follow the plan's Effect that copies formatted props into text state. | Display text is a render-time derivation; a draft is local event state. This removes prop synchronization and keeps unchanged HEX from becoming an edit. | No. |
| 2026-09-25 · Task 5 reviewer model | Use a fresh Sol reviewer. | Terra or Luna. | The review must judge input event ordering, draft preservation, and the Effect-free state shape. | No. |
| 2026-09-25 · Task 5 draft baseline | Compare a committed draft with the value and displayed text captured when editing began. | Compare to the latest props; discard draft on every external update. | An equivalent spelling should not overwrite a newer collaborative value, while an intentional distinct edit can still commit. | Yes, after browser interaction checks. |
| 2026-09-25 · Task 5 fix reviewer model | Use a fresh Sol reviewer for precision and stale-draft fixes. | Reuse the first reviewer or use Terra. | The correction spans parsing semantics and collaborative prop changes during editing. | No. |
| 2026-09-25 · Task 5 format change during draft | Preserve a live draft even if the format selection changes; Escape restores the latest prop display. | Discard the draft automatically on format change. | Both input syntaxes are self-describing and the draft remains recoverable. The reviewer classified old syntax beside the new selection as Minor UX. | Yes, after Task 7 browser checks. |
| 2026-09-25 · Task 6 implementer model | Use Sol. | Terra or Luna. | The popover composes several interactive controls, canvas painting, accessible sliders, and visual reference matching. | No. |
| 2026-09-25 · Task 6 hue pointer | Preserve the exact `hueValue()` result at pointer top, including 360°. | Follow the plan's `% 360` normalization in the pointer handler. | Task 3's reviewed geometry maps 360° to the strip top; modulus would reintroduce the thumb jump. | No. |
| 2026-09-25 · Task 6 canvas and SVG | Use `context.createImageData()` and the repository's `createElement` SVG guard while keeping the same pixel and path output. | Use the plan's direct `new ImageData()` and literal SVG JSX. | The typed array signature and repository SVG/focus guards reject the literal examples; equivalent browser output remains. | No. |
| 2026-09-25 · Task 6 reviewer model | Use a fresh Sol reviewer. | Terra or Luna. | The task combines pointer and keyboard interactions with canvas rendering and visual reference matching. | No. |
| 2026-09-25 · Task 7 implementer model | Use Sol. | Terra or Luna. | The design-audit specimen joins live color controls to a real Sparkline and requires responsive browser interaction checks. | No. |
| 2026-09-25 · Task 7 reviewer model | Use a fresh Sol reviewer. | Terra or Luna. | The review combines fixture accuracy, latest audit integration, live interaction evidence, and visual quality. | No. |
| 2026-09-25 · Slice 1 reviewer model | Use a fresh Sol reviewer across Tasks 1–7. | Rely only on task reviews; use Terra. | Cross-component value flow and browser evidence require a separate whole-slice pass before checkpoint. | No. |
| 2026-09-25 · Slice 1 format-draft note | Carry the reviewer’s Minor UX finding into browser follow-up rather than block the checkpoint. | Discard invalid drafts on format changes immediately. | Both syntaxes are self-describing and Escape recovers; no data loss or Important issue was confirmed. | Yes, in later browser passes. |
| 2026-09-25 · Task 8 implementer model | Use Terra. | Luna or Sol. | The palette reducer and selectors are specified with tests, but immutable edits, key identity, and theme separation need careful state reasoning. | No. |
| 2026-09-25 · Task 8 reviewer model | Use a fresh Sol reviewer. | Terra or Luna. | The state model underpins every later palette interaction, so independent review must probe identity, immutability, and theme boundaries. | No. |
| 2026-09-25 · Task 8 prototype-like names | Store palette edits in null-prototype records and use own-key checks. | Restrict hue and step names; keep ordinary object records. | The design spec permits arbitrary names. Inherited names such as `__proto__` and `toString` must work as ordinary hue and step keys through edit, reset, and serialization. | No. |
| 2026-09-25 · Task 8 fix reviewer model | Use a fresh Sol reviewer for the key safety fix. | Reuse the first reviewer or use Terra. | The correction changes the reducer's record shape and lookup semantics that later palette controls depend on. | No. |
| 2026-09-25 · Task 9 implementer model | Use Sol. | Terra or Luna. | The grid combines visual reference matching, roving keyboard focus, accessible labels, and popover placement geometry. | No. |
| 2026-09-25 · Task 9 reviewer model | Use a fresh Sol reviewer. | Terra or Luna. | The review must judge keyboard navigation, accessible state, reference layout, and viewport placement boundaries. | No. |
| 2026-09-25 · Task 9 cramped placement | Clamp the selected vertical popover position to viewport margins when its height fits the viewport. | Keep the plan's unbounded `below` coordinate when neither side fits; flip despite the chosen side having more room. | Review found that a 500 px popover can extend below an 800 px viewport even though it fits within the viewport itself. | No. |
| 2026-09-25 · Task 9 fix reviewer model | Use a fresh Sol reviewer for vertical placement boundaries. | Reuse the first reviewer or use Terra. | The correction affects mobile viewport behavior and a user-visible anchored interaction. | No. |
| 2026-09-25 · Task 10 implementer model | Use Sol. | Terra or Luna. | The anchored popover needs DOM geometry, native light-dismiss timing, focus return, live palette editing, and visual browser checks. | No. |
| 2026-09-25 · Task 10 reviewer model | Use a fresh Sol reviewer. | Terra or Luna. | Review must independently assess native popover timing, viewport-following layout, focus restoration, and value flow across packages. | No. |
| 2026-09-25 · Task 10 focus return | Return focus to the swatch on Escape; keep the pointer's new focus on outside light-dismiss. | Follow the plan's unconditional anchor focus on every close. | Review reproduced a click on Foundations navigating while focus jumps backward to Gray 450; that breaks pointer navigation and accessibility. | No. |
| 2026-09-25 · Task 10 input Escape | Let the first Escape cancel an active ColorField draft while keeping the popover open; a later Escape closes it. | Allow the native popover default to close on the first Escape even though the field clears its draft. | Integrated browser use showed `stopPropagation` alone does not stop the native popover dismissal; the field spec explicitly promises Escape restores the draft text. | No. |
| 2026-09-25 · Task 10 fix reviewer model | Use a fresh Sol reviewer for dismissal focus and input Escape. | Reuse the first reviewer or use Terra. | The correction spans native browser focus timing and a keyboard interaction that static rendering cannot exercise. | No. |
| 2026-09-25 · Slice 2 reviewer model | Use a fresh Sol reviewer across Tasks 8–10. | Rely only on task reviews; use Terra. | Cross-task palette identity, focus, browser geometry, and visual composition need an independent whole-slice pass before checkpoint. | No. |
| 2026-09-25 · Task 11 implementer model | Use Terra. | Luna or Sol. | Curve interpolation and palette state transitions are specified with tests, but gamut ratios, base-row idempotence, and arbitrary hue names need careful reasoning. | No. |
| 2026-09-25 · Task 11 empty curve | Give `curveFrom([])` finite zero-lightness endpoints. | Preserve the plan's `Math.min(...[])` and `Math.max(...[])` infinities; reject empty input. | The palette permits an empty hue row, and a finite fallback avoids invalid settings if a caller requests its curve. | No. |
| 2026-09-25 · Task 11 reviewer model | Use a fresh Sol reviewer. | Terra or Luna. | The review needs independent numerical and reducer-state judgment, including idempotence, gamut limits, theme isolation, and arbitrary keys. | No. |
| 2026-09-25 · Task 11 minor coverage | Carry dark-theme curve/reset and reversed-row checks into the later theme and integrated browser pass. | Add implementation-shaped tests immediately for every reducer branch. | The focused tests already cover base-derived idempotence, resets, and prototype-like hue names; the reviewer found no behavior bug. | Yes, before final review. |
| 2026-09-25 · Task 12 implementer model | Use Sol. | Terra or Luna. | Scrub drag and keyboard behavior, SVG chroma graph, native selects, and reference-matched curve controls require UI judgment. | No. |
| 2026-09-25 · Task 12 SVG element | Use the repository's guarded `Svg = "svg"` pattern for the chroma graph. | Add a literal `<svg>` as in the plan. | The token/style contract rejects a literal SVG in visuals; Sparkline already uses the guarded pattern for the same output. | No. |
| 2026-09-25 · Task 12 reviewer model | Use a fresh Sol reviewer. | Terra or Luna. | Review must judge pointer and keyboard input behavior, draft parsing, graph math, accessible controls, and reference-matched UI. | No. |
| 2026-09-25 · Task 12 scrub draft | Resolve a valid active draft as the starting value for handle drags and arrow keys, then clear it. | Step from the previous prop value; rely on input blur after pointerdown. | Review showed pointer capture prevents blur, so a stale visible draft can later overwrite a drag, and arrows can jump away from the number just typed. | No. |
| 2026-09-25 · Task 12 fix reviewer model | Use a fresh Sol reviewer for ScrubField draft resolution. | Reuse the first reviewer or use Terra. | The fix joins text, pointer, and keyboard state, and needs independent scrutiny for stale values and clamping. | No. |
| 2026-09-25 · Task 13 implementer model | Use Sol. | Terra or Luna. | Integrating the curve pane requires responsive top-layer layout, live pointer editing, focus and dismissal behavior, and visual judgment. | No. |
| 2026-09-25 · Task 13 placement subscription | Recalculate placement with a cleaned-up ResizeObserver and bound the open panel to the viewport with internal scrolling. | Recalculate only when `curveOpen` changes, as in the plan; let the tall mobile panel overflow. | Editing can change content height after opening, and both panes stacked exceed 390×844; browser checks showed a 16 px bound with the scrollable panel. | No. |
| 2026-09-25 · Task 13 test location | Put the selected and one-swatch toggle regression in `palette-editor.test.tsx`. | Add it to the design-audit specimen test named by the plan. | The component test directly renders the two state fixtures and checks the accessible toggle without coupling to the audit page. | No. |
| 2026-09-25 · Task 13 reviewer model | Use a fresh Sol reviewer. | Terra or Luna. | The integration combines responsive DOM layout, live curve transitions, focus, and accessible states that need independent UI judgment. | No. |
| 2026-09-25 · Task 13 stable popover | Keep the ColorPopover under one persistent wrapper while changing curve-pane layout. | Conditionally replace the direct popover child with a paired wrapper. | Browser review showed the conditional tree remounts the toggle and drops keyboard focus to `<body>` on open and close. | No. |
| 2026-09-25 · Task 13 fix reviewer model | Use a fresh Sol reviewer for persistent focus across pane toggles. | Reuse the first reviewer or use Terra. | The correction relies on React node identity and native keyboard focus through layout changes. | No. |
| 2026-09-25 · Slice 3 reviewer model | Use a fresh Sol reviewer across Tasks 11–13. | Rely only on task reviews; use Terra. | The curve math, state changes, SVG, scrub inputs, responsive popover, and focus need one cross-task judgment before checkpoint. | No. |
| 2026-09-25 · Slice 3 `aria-controls` note | Carry the reviewer’s Minor optional toggle-to-pane relationship to the final accessibility pass. | Add an ID and `aria-controls` immediately after the whole-slice review. | The toggle already has an accessible name and `aria-expanded`, and the pane is a labeled region; the reviewer found no functional or Important accessibility defect. | Yes, at final review. |
| 2026-09-25 · Task 14 implementer model | Use Sol. | Terra or Luna. | Token retargeting joins reducer semantics to a grouped, accessible list and live anchored popover; visual interaction and fixture fidelity need judgment. | No. |
| 2026-09-25 · Task 14 reviewer model | Use a fresh Sol reviewer. | Terra or Luna. | Review must check arbitrary token keys, alias completeness, grouped select and swatch behavior, missing refs, and live state flow. | No. |
| 2026-09-25 · Task 14 long-name note | Carry long, unbroken token-name wrapping to final responsive review. | Add a layout rule now without a failing fixture or browser case. | The reviewer found the current 390 px fixture fits; token discovery with arbitrary repository names belongs to sub-project 2. | Yes, if final review finds a concrete overflow. |
| 2026-09-25 · Task 15 implementer model | Use Sol. | Terra or Luna. | The theme toggle changes visible palette, token resolution, contrast defaults, popover lifecycle, and cross-theme edit state. | No. |
| 2026-09-25 · Task 15 reviewer model | Use a fresh Sol reviewer. | Terra or Luna. | Review must assess theme isolation, synthetic fixture honesty, token missing states, native popover dismissal, and mobile visual behavior. | No. |
| 2026-09-25 · Task 15 mobile ramp | Give every step at least 32 px and scroll the headers and swatches together within the grid below 640 px. | Shrink the 13 labels until they fit; show only some step labels. | The first Task 15 review found overlapping labels at 390 px. The shared scroll keeps all names readable, aligned with their swatches, and the page itself at 390 px. | Yes, if the later jig needs a different small-screen affordance. |
| 2026-09-25 · Task 15 empty dark theme | Hide the theme toggle when the dark theme has no hue rows. | Show a Dark button that the reducer refuses to activate; allow switching to an empty theme. | An empty array provides no dark palette to edit, so the button would offer an unusable action. | No. |
| 2026-09-25 · Task 15 fix reviewer model | Use a new Sol reviewer for the mobile ramp and empty-theme corrections. | Reuse the first reviewer or use Terra. | The correction changes responsive interaction and keyboard reachability, so it needs independent UI judgment. | No. |
| 2026-09-25 · Slice 4 reviewer model | Use a fresh Sol reviewer across Tasks 14–15. | Rely only on task reviews; use Terra. | Token retargeting, active-theme resolution, mobile ramp scrolling, and popover state need one independent cross-task pass before checkpoint. | No. |
| 2026-09-25 · Whole-branch reviewer model | Use a fresh Sol reviewer against `origin/main`. | Rely on the four slice reviews; use Terra. | Final review needs cross-package API, accessibility, browser behavior, and spec judgment across all slices. | No. |
| 2026-09-25 · Visual report author model | Use Sol for the one-page HTML report. | Use Luna for mechanical assembly; author it in the orchestrator turn. | Selecting truthful before/after evidence and matching the reference visuals requires design judgment, while the orchestrator reviews and verifies the result. | No. |
| 2026-09-25 · Final interaction-fix implementer model | Use Sol for keyboard popover focus and swatch-scoped text drafts. | Use Terra or Luna. | Native popover timing, keyboard modality, and draft identity need careful UI state judgment. | No. |
| 2026-09-25 · Final graph-fix implementer model | Use Terra for chroma scaling and real step labels. | Use Sol or Luna. | The failure is a bounded numeric mapping and prop-forwarding correction with concrete test cases. | No. |
| 2026-09-25 · Final graph-fix reviewer model | Use a new Sol reviewer. | Reuse the whole-branch reviewer; use Terra. | Independent review must judge accepted out-of-gamut geometry and labels across varied step systems. | No. |
| 2026-09-25 · Final interaction-fix reviewer model | Use a new Sol reviewer. | Reuse the whole-branch reviewer; use Terra. | Independent browser and React review must check native popover focus timing, pointer behavior, and draft identity. | No. |
| 2026-09-25 · Final graph scale | Scale the chroma plot to the larger of the gamut maximum and current chroma. | Clip the current line to the graph ceiling; leave out-of-gamut values outside the SVG. | The controls accept out-of-gamut values, and the whole-branch review reproduced a negative SVG y coordinate crossing the controls. The new scale keeps both curves inside while showing current above maximum. | No. |
| 2026-09-25 · Final graph labels | Show each row's actual step labels in the chroma graph. | Keep ordinal labels from the nine-step reference. | Chopin's gray ramp uses 50–900 and has 13 steps; ordinals described the wrong palette position. | No. |
| 2026-09-25 · Final popover focus | Flush selection before showing the native popover and move focus to its first enabled control for both pointer and keyboard openings. | Focus only on keyboard opening; leave focus on the swatch and rely on Tab order. | Fresh review reproduced Tab skipping the open popover after a pointer click; a single focus path also removes modality plumbing. Outside light-dismiss still preserves the newly focused target. | No. |
| 2026-09-25 · Final text draft identity | Key only `ColorField` by theme, hue, and step while keeping the parent format selection. | Keep one field instance for all swatches; key the entire popover. | Whole-branch review reproduced invalid text from Gray 450 on Gray 500. A field-only key drops stale draft and validation state without resetting the user's selected format or the rest of the popover. | No. |
| 2026-09-25 · Final react-dom contract | Add `react-dom` as a visuals peer dependency and refresh `bun.lock`. | Rely on a dev dependency or an app's incidental install. | The focus fix imports `flushSync` at runtime, so the package contract should declare that dependency. | No. |
| 2026-09-25 · Final interaction second-review model | Use another fresh Sol reviewer for pointer focus, draft identity, and package metadata. | Reuse either previous reviewer; use Terra. | The first correction still left a pointer Tab path outside the popover; a new reviewer checks both modalities and the runtime boundary. | No. |
| 2026-09-25 · Final whole-branch re-review model | Use a new Sol reviewer after the graph and interaction fixes. | Treat the focused fix reviews as the final whole-branch verdict; use Terra. | The first branch-wide review found three Important issues; a fresh integrated pass should judge the corrected branch together before the final commit. | No. |
| 2026-09-25 · Visual report assets | Keep original reference PNGs linked from `specs/assets`, commit only the browser screenshots used by the report, and embed the local Inter font in its HTML. | Duplicate the references in `reports/color-controls`; load a network font; commit every exploratory screenshot. | The report remains a portable local page with no network dependency, while each reference has one tracked source and the evidence bundle contains only the captures readers see. | No. |
